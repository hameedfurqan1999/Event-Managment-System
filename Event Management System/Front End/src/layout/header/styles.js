import { makeStyles } from '@material-ui/core/styles'

const useStyles = makeStyles({
  navLink: {
    color: '#fff',
    fontSize:'15px',
    padding:'20px'
  },
  right: {
    marginLeft: 'auto',
  },
})

export { useStyles }
